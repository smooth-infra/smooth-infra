---
version: 1
input:
  terraform:
    outputs_file: terraform/output.vars
tests:
  - name: Make sure that there is at least one EC2 deployed with tag key Name and value Test
    type: aws-search
    params:
      resource_type: ec2
      tags:
        - key: Name
          value: Test
    expects:
      count: 1